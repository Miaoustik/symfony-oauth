<form action="{{ socialLink }}" data-turbo="false" class="w-full">
    <button type="submit" class="flex w-full text-gray-900 bg-primary-600 border border-gray-900 hover:bg-gray-400 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:text-white dark:bg-primary-600 dark:hover:bg-primary-200 dark:focus:ring-primary-800 flex justify-center items-center dark:border-white">
        {% block socialIcon %}

        {% endblock %}
        <span>{{ socialText }}</span>
    </button>
</form>
